---
description: Workspace boundary and output path policy for the eloftr project. Server side (vlrlab, root, shared account xyjiang) enforces extra blacklist; local Windows side just stays inside the repo root.
alwaysApply: true
---

# 写入边界

## 服务器（Linux, vlrlab, 仓库根 `/home/xyjiang/Desktop/yurupeng/eloftr/`）

- **可写**：`/home/xyjiang/Desktop/yurupeng/` 整个子树
- **只读软链复用**（仅作软链目标）：
  - `/data/xyjiang/Datasets/Infrared_image_datasets/TarDAL/{M3FD_Detection,roadscene,tno}/`
  - `/data/xyjiang/Datasets/`、`/data/xyjiang/Matching/EfficientLoFTR/`（备查）
- **黑名单**（禁止任何写）：
  - `/home/xyjiang/{anaconda3,.bashrc,.condarc,.gitconfig,.cursor,.cursor-server,projects}/`
  - `/data/` 整盘（除上述只读复用外）
- **进一步约束**：服务器侧仅 ask mode 工作，不调任何写工具/Shell。详见 `.cursor/rules/01-server-ask-only.mdc`。

## 本地 Windows（仓库根 `c:\Users\abirdlikefish\Desktop\毕设\efficient_loftr\`）

- **可写**：仓库根整树（agent mode 自由开发的主战场）
- 无 `/data/`、无 `/home/xyjiang/.cursor/` 等 Linux 专属黑名单路径，不会触发误判
- 推荐工作流：agent 改完 → `git push` → 服务器 `git pull --ff-only` 后跑训练（详见 `abfNote/dev-workflow.md`）

> 触发越界写入需求时，**先用 AskQuestion 显式问用户**，得到许可再做。

# 输出物落位（违反即视为越界）

| 类别 | 路径 |
|---|---|
| skill | `.cursor/skills/<name>/SKILL.md` |
| rule | `.cursor/rules/<name>.mdc` |
| hook | `.cursor/hooks.json` + `.cursor/hooks/<name>.{py,sh,bat}`（跨平台优先 `.py`） |
| plan / todo 阶段总结 | `~/.cursor/plans/<name>_<hash>.plan.md`（Cursor 全局，**不**双写到项目）|
| 对话归档 | `.cursor/chat-logs/<uuid>.jsonl`（由 `.cursor/hooks/archive_transcript.py` 在 stop/sessionEnd 时自动归档，跨平台、`failClosed:false`） |
| 人工备忘 | `abfNote/chat-logs/<YYYYMMDD>.md` |
| 工程笔记 | `abfNote/<topic>.md` |
| 训练日志 | `logs/`（仓库内实体目录） |
| ckpt | `weights/`（仓库内实体目录） |
| 数据集 | `data/<dataset>/` 软链到只读源 |
| PC 缓存 | `data/pc_cache/<dataset>/{Ir_pc,Vis_pc}/` |
| 跨版本 eval 汇总 | `results/eval_summary.md`（入版本库；维护规则见 `.cursor/skills/eloftr-results/SKILL.md`） |
| 跨实验 TB 训练侧汇总 | `results/tb_summary.md`（入版本库；维护规则见 `.cursor/skills/eloftr-tb-summary/SKILL.md`） |

# 禁令

- 禁止 `sudo` / `su` 二次提权（已是 root）
- 禁止 `pip install` 到 `base` 或导师 env（先 `conda activate eloftr_yurupeng`）
- 禁止 `rm -rf` 白名单外路径
- 禁止 `chown -R` / `chmod -R` 整目录
- 禁止往 `/data/` 任何子目录写、新建、删除
- 禁止占满 4 卡：`CUDA_VISIBLE_DEVICES=N[,M]` 限定，运行前 `nvidia-smi` 检查
- 禁止把 PC-CLAHE 缓存写入只读源目录 `data/M3FD_Detection/Ir_pc/` 等，必须落 `data/pc_cache/<dataset>/`

详见仓库根 `AGENTS.md`。
